% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decay_climate.R
\name{bioclim_warmest_quarter}
\alias{bioclim_warmest_quarter}
\title{Look up warmest-quarter bioclim variables for a location}
\usage{
bioclim_warmest_quarter(
  lat,
  lon,
  res = c("10", "5", "2.5"),
  path = NULL,
  quiet = FALSE
)
}
\arguments{
\item{lat, lon}{Latitude and longitude in decimal degrees (WGS84).}

\item{res}{WorldClim spatial resolution in arc-minutes: \code{"10"} (default,
~18 km, smallest download), \code{"5"}, or \code{"2.5"} (~4.6 km, finer but larger).}

\item{path}{Directory to cache downloaded layers. Defaults to a per-user
cache directory.}

\item{quiet}{If \code{TRUE}, suppress the download progress message.}
}
\value{
A named numeric vector with \code{temp_warmq_C} (BIO10) and
\code{precip_warmq_mm} (BIO18).
}
\description{
Downloads (and caches) WorldClim 2.1 climate layers and extracts the mean
temperature of the warmest quarter (BIO10, deg C) and precipitation of the
warmest quarter (BIO18, mm) at a point. Requires the \pkg{terra} package and
an internet connection on first use; downloaded layers are cached for reuse.
}
\examples{
\dontrun{
# Requires terra + internet (downloads ~48 MB on first call)
bioclim_warmest_quarter(lat = 40.0, lon = -105.3)
}
}
